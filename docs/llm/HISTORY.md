# LLM Change History

Append new entries at the top so the most recent activity is easiest to find. Follow the required format:

YYYY-MM-DD - <LLM_NAME> - <Brief summary> - Files: [list of touched files] - Version impact: <yes/no + details>

## Log

### Example Entry Format

```
2025-01-15 - Claude - Add authentication module with JWT support - Files: [src/auth/jwt.js, src/auth/middleware.js, tests/auth.test.js, docs/llm/HANDOFF.md] - Version impact: yes (src/auth/jwt.js -> 1.1.0, breaking change requires new ENV var JWT_SECRET)
```

### Your Project History

- 2026-05-03 - Claude Opus 4.7 (1M context) (proposer) + GPT-5 (critic) + Carlos (arbiter) - **Doc-only addition: `docs/CONSENSUS_PROTOCOL_PROPOSAL.md` + structured `docs/llm/REVIEWS.md` audit-trail entry. No version bump.** A consensus run on 2026-05-03 (5 rounds) formalised the propose / critique / arbitrate pattern — already in informal use during the construction of operator's `tomatic` project — as a named primitive of LLM-DocKit. The proposal defines roles, invocation thresholds (contract changes / multi-repo / security/persistence / multi-week reversibility / precedent-setting; explicitly NOT for typo fixes / dependency bumps / single-file refactors), mechanics (N rounds, classify outcome as closed-accepted / closed-rejected / open-next-round / deferred, terminate when arbiter classifies closed), the structured `REVIEWS.md` recording format that preserves causality (decision / proposer / critic / arbiter / rounds / outcome + per-decision: short name, who proposed, objection considered, why this resolution, risk accepted, implementing artefact), failure modes (non-convergence / arbiter unavailable / decision later overturned / critic capture) with explicit mitigations, and the explicit relationship with the existing `DOWNSTREAM_FEEDBACK.md` + `*_PROPOSAL.md` channels — the consensus protocol consumes DF entries and produces proposals; it does NOT replace the feedback channel. Sibling proposal `docs/DEPLOYMENT_EVIDENCE_PROPOSAL.md` filed in `home-infra-protocol` (the same consensus run produced both); the deliberation that produced the cross-repo proposals is recorded in REVIEWS of both repos with bidirectional cross-references. DF-029 in this repo moved to `partially accepted` (process side resolved by this proposal; validator side `--check deployed-version` remains open). ForgeOS mentioned in the proposal as future consumer / precedent, NOT as design requirement (rejected during the run by GPT-5: "no convertir ForgeOS en requisito formal"). NO bump because only `docs/` paths changed (excluded from the pre-commit hook's bump trigger), consistent with how the previous `HOOKS_ENFORCEMENT_PROPOSAL.md` and `LLM_DOCKIT_CE_V2_PROPOSAL.md` proposals live untracked in this repo. Adopter count: 29 entries (DF-029 added 2026-05-03). The first project to adopt the protocol formally is Tomatic; once a future patch ships the template-side changes (LLM_START_HERE block + REVIEWS template + README pointer), Tomatic's `LLM_START_HERE.md` should be re-synced from the new template. - Files: [docs/CONSENSUS_PROTOCOL_PROPOSAL.md, docs/DOWNSTREAM_FEEDBACK.md, docs/llm/REVIEWS.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (doc-only addition; no scaffold logic / validator / template change yet)
- 2026-05-01 - Claude Opus 4.7 (1M context) - Cut v4.6.1 (patch) — one-line fix in `scripts/dockit-init-project.sh` so new projects are born on `main`, not `master`. The script previously called `git init -q` without specifying a branch, which inherited the host system's `init.defaultBranch` setting; on systems where that defaults to `master`, the first commit landed on `master` while the orchestrator script and `/new-homelab-project` skill in `home-infra-protocol` documented `git push origin main` — a fresh project would have failed at first push. Found by GPT-5 review during smoke testing of the orchestrator. Fix: added `git symbolic-ref HEAD refs/heads/main` immediately after `git init -q`. Portable to Git < 2.28 (no dependency on the `-b main` flag from Git 2.28+). Re-smoke-tested in `/tmp/smoke-init`: `git branch --show-current` now returns `main`, validator still passes 6/6. v4.6.0's broader feature description (init script + HOW_TO_USE reorganization + DocKit-meta-file pruning + git-archive copy) carries forward unchanged. Bumped 4.6.0 → 4.6.1 (patch). - Files: [scripts/dockit-init-project.sh, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 5 doc-version html-comment markers] - Version impact: yes (4.6.0 → 4.6.1, patch: branch-default fix in init script)
- 2026-05-01 - Claude Opus 4.7 (1M context) - Cut v4.6.0 (minor) shipping `scripts/dockit-init-project.sh`, the from-scratch project initializer that closes the gap between `/adopt-dockit` (existing repos) and the manual instructions in `HOW_TO_USE.md`. One command produces a clean, validator-green project at `0.1.0` ready for first session. Behavior: validates inputs (slug pattern, target absent, source is a git repo with VERSION) → `git archive HEAD | tar -x` from the source DocKit checkout into target (tracked files only, so untracked drafts in the source working tree never leak — same risk class as DF-027) → strips DocKit-internal meta files (`HOW_TO_USE.md`, `docs/DOWNSTREAM_FEEDBACK.md`, `docs/EXTERNAL_CONTEXT_PLUGIN_PLAN.md`, `dockit-sync-manifest.yml`, `scripts/dockit-sync.sh`, `scripts/dockit-sync-check.sh`, `scripts/dockit-init-project.sh` itself) → resets live operational docs (`CHANGELOG.md`, `docs/llm/HANDOFF.md`, `docs/llm/HISTORY.md`, `docs/llm/DECISIONS.md`) to fresh stubs so DocKit's own DF-027 / plaud-mirror / D-001..D-006 content does not leak → substitutes `<PROJECT_NAME>` / `<CONVERSATION_LANGUAGE>` / `<YYYY-MM-DD>` placeholders in remaining `*.md`/`*.yml`/`*.json` → runs `scripts/bump-version.sh 0.1.0` to set VERSION and sync doc-version markers atomically → initializes a fresh git repo with a single `chore: initial scaffold from LLM-DocKit X.Y.Z` commit. Strict scope: NO GitHub remote creation, NO push, NO ecosystem-profile application, NO `~/.claude/` edits. Those concerns belong to higher-layer orchestrators (such as a `home-infra-protocol/integrations/dockit/new-homelab-project.sh` that calls this script as its first step and layers GitHub creation + profile application on top). Smoke-tested in `/tmp/smoke-init` over three iterations: caught two real bugs before commit — (a) `tar` from the working tree leaked five untracked DocKit drafts into the new project (`documento.md`, `LLM_DOCKIT_CE_V2_PROPOSAL.md`, `HOOKS_ENFORCEMENT_PROPOSAL.md`, `DEFERRED_NEXT_VERSION.md`, `EXTERNAL_CONTEXT_PLUGIN_PLAN.md`); switched to `git archive HEAD` and added a hard-fail when source has no `.git`; (b) the fresh `CHANGELOG.md` had no `## [` anchor for `bump-version.sh` to insert before, leaving the new project's `[0.1.0]` section empty; fixed by emitting the `## [0.1.0]` section directly in the stub. Final smoke run: validator 6/6 PASS, 8 doc-version targets in sync at 0.1.0, single-commit git history. `HOW_TO_USE.md` reorganized so the new "Quick Start (one command, recommended)" section ships above the existing manual flow, which is preserved as "Quick Start (manual)" for Windows / CI environments without the helper script. The previously speculated v4.6.0 plan from the v4.5.5 HANDOFF (three concrete pre-commit checks: DF-027(b) staged-imports-resolve + DF-002 orphan-marker scan + DF-008 empty-CHANGELOG guard) is **deferred to v4.7.0** and acknowledged in HANDOFF as a separate, orthogonal workstream — that pre-commit hook work targets failure modes already observed in the field but is unrelated to from-scratch initialization. Bumped 4.5.5 → 4.6.0 (minor: new backward-compatible feature). - Files: [scripts/dockit-init-project.sh, HOW_TO_USE.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 5 doc-version html-comment markers (README, LLM_START_HERE, docs/PROJECT_CONTEXT, docs/ARCHITECTURE, docs/VERSIONING_RULES)] - Version impact: yes (4.5.5 → 4.6.0, minor: new from-scratch init script)
- 2026-04-25 - Claude Opus 4.7 - Cut v4.5.5 to add DF-027 ("`git add -u` silently skips new untracked files; commit succeeds, workspace stays green, origin is broken") after the adopter project (plaud-mirror) shipped a broken v0.4.17 release and forward-fixed it in v0.4.18 (commit `d2f17f2`). DF-027 specialises DF-024 ("documenting without verifying") at the git-mechanics layer: distinct enough from DF-014 ("commit accumulation across version bumps") to deserve its own entry. Concrete failure: LLM creates new files with the Write tool; runs `git add -u && git commit -m "..."`; `-u` only stages MODIFIED tracked files, so the new files stay `??`-untracked; commit succeeds with whatever was modified-and-tracked, references the new files in commit message and CHANGELOG narrative, pushes cleanly. Local workspace remains green because `tsc -b` reads source files from filesystem (not git), `node --test` reads compiled output the same way, `docker compose build` uses `COPY . .` which copies the missing-from-git files AS IF they had been versioned, and `dockit-validate-session.sh` checks markers/dates/sync pairings — none inspect git tracking state of source files. The only signal during the commit was the stat line (plaud-mirror's was `128 insertions, 183 deletions` for a release whose narrative claimed ~500 added lines of helpers + tests — net negative is incompatible with that claim) and the LLM read past it. GPT-5 caught it via `git status` on a clean checkout + grepping for orphan references in tracked files. A fresh clone of the broken commit would have failed `npm install && npm run build` at import-resolution. Three protocol mitigations proposed in DF-027 body: (a) LLM convention — already adopted in plaud-mirror's auto-memory as `feedback_git_status_post_stage.md` ("post-stage, run `git status` and confirm zero untracked source/test files; post-commit, read the stat line and check magnitude is consistent with the release narrative"); (b) **stretch pre-commit hook check** that greps the staged tree for relative-path imports referencing files NOT in the staged set, errors if any are missing — would catch DF-027 mechanically with low false-positive risk (skip dynamic imports / module aliases); (c) further-stretch advisory hook that warns on net-negative stat line for commits whose message contains "add"/"feature". DF-027 ships `partially implemented (plaud-mirror v0.4.18)` on the adopter-symptom axis (the missing files are now in the committed tree, `git ls-tree -r HEAD packages/shared/src/` confirms; the v0.4.17 tag stays as known-broken with a CHANGELOG entry under `### Fixed` naming the reality), protocol-level pre-commit hook is `open`. Adopter count: 27 entries, 5 implemented, 2 partially implemented (DF-026 + DF-027 — partially-implemented count plural now, validating the v4.5.4 legend extension), 20 open. Doc-only release; no validator or template change in this patch. The actionable next bump candidate is v4.6.0 (minor): land three concrete pre-commit checks at once — DF-027(b) staged-imports-resolve, DF-002 orphan-marker scan, DF-008 empty-CHANGELOG guard. All three target field-observed failures, together they would have prevented two of plaud-mirror's recent broken releases without relying on LLM discipline. Bumped 4.5.4 → 4.5.5 (patch: DF-027 addition + matching HANDOFF/HISTORY/CHANGELOG narrative). - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION, README.md, LLM_START_HERE.md, HOW_TO_USE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.5.4 → 4.5.5, patch: DF-027 addition closing the loop on plaud-mirror's broken-release incident)
- 2026-04-24 - Claude Opus 4.7 - Cut v4.5.4 to update DF-026's Status from `open` to `partially implemented (plaud-mirror v0.4.17)` and formalise `partially implemented (<adopter-version>)` as a third Status value in the file's Status legend. Trigger: plaud-mirror v0.4.17 (commit d1bc317 on origin/main) extracted the previously local-to-App.tsx pure UI helpers (formatDuration, formatBytes, formatRecordingsMetric, computeMissing, formatDeviceLabel, formatDeviceShortName, coerceNonNegativeInteger, summarizeRun, describeBusy) into packages/shared/src/formatting.ts and added 12 dedicated node:test tests hooked into the root npm test invocation. That closes the helper-level half of DF-026 — a regression in any of those helpers would now actually fail the suite. Component-level rendering tests (tabs persistence + switch, collapsible card ARIA + mount/unmount of BackfillPreview, debounced fetch behaviour) remain `open` because they would require introducing vitest+jsdom+@testing-library/react as new dependencies, which is a non-trivial scope addition deferred. Protocol-level template change (split DocKit's "every new runtime case must come with tests" rule by layer, with explicit waiver path in HISTORY for UI cases) also still `open`. The asymmetry between "fix landed in the adopter" and "protocol cure still ahead" forced an honest Status taxonomy: `open` would have understated the adopter fix, `implemented` would have overclaimed the protocol cure. Solution: add `partially implemented (<adopter-version>)` formally to the legend. The two-axis pattern is observed in many DF entries — DF-001/002/003/025 happened to close both axes simultaneously because the protocol work was a one-off doc fix; DF-026 split because the protocol work is a real template change requiring future thinking. Future entries with both axes should default to `partially implemented` when only one is closed. v4.5.4 doc-only release. Bumped 4.5.3 → 4.5.4. Adopter count after this release: 26 entries, 5 `implemented`, 1 `partially implemented`, 20 `open`. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION, README.md, LLM_START_HERE.md, HOW_TO_USE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.5.3 → 4.5.4, patch: DF-026 status update + Status legend extension)
- 2026-04-24 - Claude Opus 4.7 - Cut v4.5.3 to add DF-025 and DF-026 after a third GPT-5 review of plaud-mirror v0.4.15 surfaced two more patterns. DF-025 ("Runbook promises a configuration the codebase doesn't actually support") is a specialisation of DF-024: the drift is code-vs-docs rather than doc-vs-doc. The plaud-mirror instance that surfaced it: v0.4.15 rewrote DEPLOY_PLAYBOOK listing `node:20-alpine` as an acceptable Docker fallback with a literal `export ...=node:20-alpine` example, while the committed Dockerfile forced `SHELL ["/bin/bash", "-lc"]` on both stages and Alpine ships no bash. An operator following the runbook would have hit a build error at step zero. Same DF-024 pattern: the LLM documented without verifying. Closed in plaud-mirror v0.4.16 (commit 7110cf6) by removing both SHELL directives and verifying Alpine build end-to-end including `GET /api/health` 200. DF-025 ships as `Status: implemented (plaud-mirror v0.4.16)` on the adopter-symptom axis; the protocol-level `Verified: <commit-sha>` annotation convention for concrete runbook examples is `open`. DF-026 ("Backend tests go green while UI-state features ship with only a build-shell smoke test") captures a layer-asymmetry in the existing DocKit rule "every new runtime case must come with explicit tests." Adopter LLMs interpret it as "backend tests count" — plaud-mirror v0.4.14 shipped tabs + collapsible backfill + localStorage with 53/53 passing even though the only web-side test is `tests/integration/web-build.test.mjs` which merely asserts the build emits a shell. A regression breaking any of those to non-functional would still report PASS. Not a validator problem (coverage is adopter-owned), but a template-rule gap: DocKit's LLM_START_HERE line doesn't differentiate backend-runtime from UI-state, so LLMs default to the layer they are fluent in. Proposed template change: split the testing rule by layer and offer an explicit waiver path in HISTORY for UI cases left untested, so the gap is acknowledged rather than silently skipped. No adopter fix attached to DF-026 because the cure is a DocKit template change. Also added a third meta-pattern observation at HANDOFF Current Status level (not yet promoted to the file's meta-observation block): alongside "structural vs semantic" and "single-fact-in-multiple-places", **code-vs-docs drift** (DF-001 kind-of, DF-007, DF-025) is forming as a third strand. Too early to call it distinct vs a specialisation of the existing two, so it stays at HANDOFF-level until the next pass. Bumped 4.5.2 → 4.5.3 (patch: content addition + DF-025 adopter-symptom closed). Count: 26 entries now, 5 `implemented` (DF-005, 001, 002, 003, 025), 21 `open`. Still no validator or template change in this patch. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION, README.md, LLM_START_HERE.md, HOW_TO_USE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.5.2 → 4.5.3, patch: DF-025 + DF-026, DF-025 marked implemented on adopter axis)
- 2026-04-24 - Claude Opus 4.7 - Cut v4.5.2 to add DF-024 — "Documenting drift is not fixing drift" — after a second GPT-5 review on 2026-04-24 pointed out that the three concrete drifts which had been catalogued as DF-001 (DEPLOY_PLAYBOOK Kali bash block), DF-002 (HOW_TO_USE.md orphan marker stuck at v0.1.0 content), and DF-003 (HANDOFF "Next: rebuild + push" stale) at LLM-DocKit v4.5.0 were STILL BROKEN in the plaud-mirror adopter repo as of v0.4.14, despite the DF entries existing. The LLM had treated writing a polished DF entry as equivalent to repairing the instance. This release names that anti-pattern: DF-024 records it as distinct from DF-010 (in DF-010 the LLM hasn't noticed the content problem; in DF-024 the LLM HAS noticed and turned the noticing into a governance artifact without closing the loop), proposes three non-exclusive mitigations (convention requiring DF entries citing file:line to ship with fix-commit OR explicit TODO block; stretch validator check cross-referencing DF entries against adopter git log; session-end ritual auditing every DF written in the session for fix-vs-Status truthfulness), and models the convention by updating DF-001/002/003 Status lines inline to read `implemented (plaud-mirror v0.4.15)` with pointers to the concrete commits that closed each instance. The plaud-mirror fixes themselves shipped in plaud-mirror v0.4.15 (commit 94e9edb on origin/main): DEPLOY_PLAYBOOK fallback block rewritten to the acceptable-substitutes list with a pointed rejection paragraph for pentesting/general-purpose distros, HOW_TO_USE.md fully rewritten from v0.1.0 "design baseline" body to v0.4.15 reality body AND added to docs/version-sync-manifest.yml (20 targets, up from 19) as the structural fix against recurrence, HANDOFF Current Status + LLM_START_HERE Current Focus cleared of trailing "Next: rebuild + push" text. Three of the 24 DF entries moved from `open` to `implemented (plaud-mirror v0.4.15)`; together with DF-005 that makes four implemented, twenty open. Meta-observation at the bottom of DOWNSTREAM_FEEDBACK unchanged in wording — DF-024 is the through-line of this release, threaded below the last prior entry. Updated HANDOFF Current Status to reflect the 24-entry count and the new DF-024; LLM_START_HERE is a template in this repo so is not touched. Bumped 4.5.1 → 4.5.2 (patch: content addition + status corrections, no structural change). Still no validator or template change; the value of this minor is epistemic — the LLM learned a failure mode of its own, and the convention/check proposed in DF-024 is the preparatory input for whichever future bump decides to make it enforceable. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION, README.md, LLM_START_HERE.md, HOW_TO_USE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.5.1 → 4.5.2, patch: DF-024 + Status updates on DF-001/002/003)
- 2026-04-24 - Claude Opus 4.7 - Second pass on `docs/DOWNSTREAM_FEEDBACK.md`: expanded from 16 to 23 seed entries after user explicitly asked for two more thorough sweeps to find anything the first pass missed. The first pass focused on discrete failures GPT-5 had flagged plus personal-memory feedback from plaud-mirror v0.4.x; this second pass focused on LLM-native workflow patterns the first batch did not address. Seven new entries: DF-017 context compaction can lose detail the LLM wrote to persist (docs/llm/* is meant to be durable but DocKit does not prescribe WHEN to checkpoint them, so LLMs rely on lossy auto-summaries; proposes PreCompact hook nudging to checkpoint); DF-018 LLM personal auto-memory and docs/llm/ drift as two parallel stores (the 7 feedback_*.md files in the plaud-mirror LLM's auto-memory were invisible to DocKit until the user asked explicitly; proposes recommendation or /export-memory skill that bridges the two persistence stores); DF-019 cross-LLM review metadata is unstructured prose (GPT-5 reviews interleaved with Claude work left only narrative in HISTORY; proposes a standardized grep-able header block for REVIEWS.md entries with Reviewer-LLM, Reviewed-Version, Findings, Accepted, Implemented-In fields); DF-020 validator has no graduated execution modes (every invocation runs every check; proposes --mode fast|normal|strict|paranoid, with fast running marker+version-sync only, strict adding semantic checks from DF-001/006/008/013, paranoid adding structural checks from DF-002/004/018); DF-021 external-context checks are edit-triggered but not version-correlated (an adopter can bump VERSION without updating external docs and the validator stays quiet; proposes version-probe pattern in .dockit-config.yml's update_triggers using %VERSION% substitution); DF-022 HISTORY entry quality varies with no guardrail (some entries are surgical, others vague; proposes non-blocking lint for file-list, version-impact, length, and numerical-assertion signals); DF-023 DocKit uses DocKit on itself, with no external reviewer (DEFERRED_NEXT_VERSION, HOOKS_ENFORCEMENT_PROPOSAL, LLM_DOCKIT_CE_V2_PROPOSAL were still untracked in the working tree at v4.5.0; proposes that DocKit's own release process run DocKit in Strict/Paranoid mode on itself and codify CONTRIBUTING). Meta-observation at the bottom of the file was also rewritten — v4.5.0 called out one pattern ("structural vs semantic"), v4.5.1 calls out two by recognizing that DF-015/018/021 are all instances of "single fact in multiple places with no sync contract", which is a distinct class from DF-001/006/008/011/016/019 ("the artifact is shaped correctly but says the wrong thing"). Also corrected a pre-existing drift in HANDOFF: the Project Summary block carried "Current version: 4.4.0" as prose at v4.5.0; replaced with a pointer to VERSION file as single source of truth. Bumped 4.5.0 → 4.5.1 (patch: seed additions + drift fix, no structural change). - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION, README.md, LLM_START_HERE.md, HOW_TO_USE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.5.0 → 4.5.1, patch: expanded feedback log + HANDOFF drift fix)
- 2026-04-24 - Claude Opus 4.7 - Introduced `docs/DOWNSTREAM_FEEDBACK.md` as a living log of drift/gap/usability/process issues surfaced when DocKit is adopted by real projects. Scope is intentionally "what the adopter stubbed its toe on that the protocol could have prevented", not "what the adopter chose to do with the protocol". Seeded with 16 entries (DF-001..DF-016) distilled from `plaud-mirror`'s v0.1.0 → v0.4.13 experience over April 2026: four came from an explicit GPT-5 review at v0.4.13 (DEPLOY_PLAYBOOK.md bash block still recommending `vxcontrol/kali-linux` after the policy was reversed everywhere else; HOW_TO_USE.md with `<!-- doc-version -->` marker but absent from the sync manifest and stuck at v0.1.0 prose at v0.4.13; HANDOFF Current Status carrying `Next: rebuild + push` after the push happened; asymmetric enforcement between circuit-core docs and peripheral runbooks); the remaining twelve were distilled from personal-memory feedback the downstream LLM assistant had saved to itself across the same period. Categories used: `drift` (content inconsistent, marker/date checks do not catch), `gap` (class of file/behaviour the validator does not track at all), `usability` (rule exists but easy to violate), `process` (workflow mismatch around DocKit). Status values: `open` / `accepted` / `implemented` / `rejected` / `superseded-by: DF-NNN`. Entries never get deleted — implemented/rejected become audit trail. `DF-005` is already `implemented` (closed by the existing `handoff-start-here-sync` check); the other fifteen are `open`. File placement: peer to the existing planning docs (`DEFERRED_NEXT_VERSION`, `HOOKS_ENFORCEMENT_PROPOSAL`, `LLM_DOCKIT_CE_V2_PROPOSAL`, `EXTERNAL_CONTEXT_PLUGIN_PLAN`) under `docs/`, not in `docs/llm/` (not per-session working memory). Explicit `skip` entry added to `dockit-sync-manifest.yml` so the log stays DocKit-only and is never propagated to adopters. Meta-observation at the bottom of the file: DocKit currently enforces structural discipline (markers, dates, manifest membership, paired-file sync) excellently and semantic discipline not at all; every implemented check is about artifact shape; every open entry is about artifact content. That observation is the through-line the next round of validator/template work should answer. No validator or template change ships in this minor — the log is preparatory backlog. Bumped 4.4.0 → 4.5.0 (minor: new doc + new process). - Files: [docs/DOWNSTREAM_FEEDBACK.md, dockit-sync-manifest.yml, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION, README.md, LLM_START_HERE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/VERSIONING_RULES.md] - Version impact: yes (4.4.0 → 4.5.0, minor: new feedback-intake document + dockit-sync skip entry)
- 2026-03-23 - Claude Opus 4.6 - Read-only session: project status review and roadmap assessment. Reviewed HANDOFF, HISTORY, VERSION, DEFERRED_NEXT_VERSION. Summarized completed Phase 1, identified pending items (git tags v4.3.0/v4.4.0, uncommitted changes, pilot sessions), outlined short/medium/long-term priorities. No code changes. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (status review only)
- 2026-03-08 - Claude Opus 4.6 - Read-only session: comparative analysis of affaan-m/everything-claude-code (ECC) vs LLM-DocKit. ECC is a performance optimization system for AI agents (65 skills, 40 commands, 16 agents, Node.js). LLM-DocKit is a governance scaffold (POSIX shell, zero deps, enforcement cascade). Conclusion: complementary — ECC optimizes within-session productivity, LLM-DocKit ensures between-session continuity. Key differentiators: LLM-DocKit has dockit-sync and portable enforcement; ECC has domain skills and multi-harness support. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (analysis only)
- 2026-03-07 - Claude Opus 4.6 - Read-only session: reviewed project status, summarized completed Phase 1 work, identified next steps (pilot 10 sessions, evaluate B/C initiatives, downstream rollout). No code changes. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (status review only)
- 2026-03-02 - Claude Opus 4.6 - Fix bootstrap bug: dockit-validate-session.sh was deleted during bootstrap but still referenced by .claude/settings.json hooks, causing Stop hook to block every response. Added script + .claude/settings.json + .claude/rules/ to dockit-sync-manifest.yml with copy strategy. Updated HOW_TO_USE.md section 4 to warn against deleting the validation script and use selective file removal instead of rm -rf scripts/. - Files: [dockit-sync-manifest.yml, HOW_TO_USE.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (bug fix in docs/manifest, no code change to scripts)
- 2026-03-01 - Claude Opus 4.6 - Created /adopt-dockit skill for adding LLM-DocKit scaffold to existing repos. Skill instructs the LLM to analyze the project (README, structure), copy template files (always/if-missing/create-new categories), replace placeholders, personalize docs with intelligence (real PROJECT_CONTEXT, STRUCTURE, HANDOFF), run technical setup (bump-version, pre-commit hook, .gitignore), validate, and optionally configure external context. Updated STRUCTURE.md with new skill. Bumped to v4.4.0. - Files: [.claude/skills/adopt-dockit/SKILL.md, docs/STRUCTURE.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION] - Version impact: yes (4.3.0 -> 4.4.0, minor: new feature)
- 2026-03-01 - Claude Opus 4.6 - Implemented External Context v1.1. Added check_external_triggers (WARN on git diff matching update_triggers globs), WARN status support in validator, --claude-rules flag in generator (produces .claude/rules/ with glob frontmatter, no paths). Fixed silent-skip bug (explicit PASS instead of bare return). Created git tags v4.0.0/v4.1.0/v4.2.0. Bumped to v4.3.0. - Files: [scripts/dockit-validate-session.sh, scripts/dockit-generate-external-context.sh, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION] - Version impact: yes (4.2.0 -> 4.3.0, minor: new feature)
- 2026-03-01 - Claude Opus 4.6 - Implemented External Context plugin v1. Created dockit-generate-external-context.sh (POSIX sh, config parser, --dry-run/--apply, idempotent marker insertion). Added check_external_context() + 3 parser helpers to validator. Added DOCKIT-EXTERNAL-CONTEXT markers to LLM_START_HERE.md. Updated sync manifest, STRUCTURE.md. Added D-006. Bumped to v4.2.0. - Files: [scripts/dockit-generate-external-context.sh, scripts/dockit-validate-session.sh, LLM_START_HERE.md, dockit-sync-manifest.yml, docs/STRUCTURE.md, docs/llm/DECISIONS.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION, CHANGELOG.md] - Version impact: yes (4.1.0 -> 4.2.0, minor: new feature)
- 2026-03-01 - Claude Opus 4.6 - Designed External Context plugin (infrastructure plugin). Two-layer: v1 (generation + existence checks), v1.1 (trigger WARN + Claude rules). Config in .dockit-config.yml, separate DOCKIT-EXTERNAL-CONTEXT markers, idempotent insertion. Reviewed and approved by human + GPT with 2 cautelas (CI portability, parser contract). Implementation deferred to next version. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (design only)
- 2026-03-01 - Claude Opus 4.6 - Fixed 7 issues from cross-LLM QA (GPT + Claude review of Phase 1): pre-commit Check 2 path exclusions (.claude/, .github/, docs/), --project bug in validator (cd to PROJECT_ROOT for version-sync), double execution of check-version-sync.sh, JSON escaping (newlines/backslashes), HANDOFF stale version (4.0.0->4.1.0) and priorities, added D-005 for version-bump-per-commit policy. - Files: [scripts/pre-commit-hook.sh, scripts/dockit-validate-session.sh, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, docs/llm/DECISIONS.md] - Version impact: no (bug fixes, no version bump)
- 2026-03-01 - Claude Opus 4.6 - Added version-bumped blocking check to pre-commit hook (Check 2: code/config staged without VERSION = ERROR). Added .ps1 to file extensions. Updated LLM_START_HERE.md template with per-commit versioning requirement. Renumbered checks (5 total). - Files: [scripts/pre-commit-hook.sh, LLM_START_HERE.md, CHANGELOG.md, VERSION, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: yes (4.0.0 -> 4.1.0)
- 2026-03-01 - Claude Opus 4.6 - Implemented Phase 1 of hook-based documentation enforcement. Created dockit-validate-session.sh (portable POSIX validator, 4 checks: handoff-date, history-entry, decisions-referenced, version-sync). Created .claude/settings.json (Stop hook blocking, PostToolUse nudge, PreCompact reminder). Created .claude/rules/ and .claude/skills/update-docs/. Extended pre-commit-hook.sh with doc validation. Added .github/workflows/doc-validation.yml for CI. Cleaned HANDOFF.md with final Decision Lock. - Files: [scripts/dockit-validate-session.sh, .claude/settings.json, .claude/rules/require-docs-on-code-change.md, .claude/skills/update-docs/SKILL.md, .github/workflows/doc-validation.yml, scripts/pre-commit-hook.sh, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, docs/STRUCTURE.md] - Version impact: no (enforcement layer, no version bump needed)
- 2026-03-01 - ChatGPT (Codex GPT-5) - Added "ChatGPT Evaluation for Claude (save for response)" to HANDOFF with severity-ranked findings, concrete text replacements, scope verdict, and go/no-go recommendation for implementation readiness. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (handoff/rfc refinement only)
- 2026-03-01 - Claude Opus 4.6 - Strategic analysis of 3 pending initiatives (Hook Enforcement, CE V2, Code Factory recs). Mapped all overlaps and conflicts: 5 conflicts identified (competing CI scripts, HISTORY structure, session tracking depth, review structure, Do Not Touch vs risk tiers), plus complementary areas. Proposed layered execution path: A (enforcement) first, then selective B/C adoption based on pilot data. Rewrote HANDOFF with complete analysis for ChatGPT round 4 review. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (strategic analysis only)
- 2026-03-01 - Claude Opus 4.6 - Consolidated HANDOFF after 3 rounds of cross-LLM review. Applied GPT corrections: fixed "18→17" hook count, fixed blocking semantics (decision:"block" not ok:false), added Decision Lock for Phase 1 scope, corrected D-004 in DECISIONS.md (was SHA256 hash, actually template_version string compare). Removed debate artifacts, produced clean implementation-ready document. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md, docs/llm/DECISIONS.md] - Version impact: no (governance consolidation only)
- 2026-03-01 - ChatGPT (Codex GPT-5) - Added "ChatGPT Round 3 Evaluation for Claude" to HANDOFF RFC: findings by severity, risks, exact replacement text, explicit answers to Claude round-2 questions, and confirmed source links for Claude Code/Cursor/Windsurf/Aider portability. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (handoff/rfc refinement only)
- 2026-03-01 - Claude Opus 4.6 - Round 2 cross-LLM review. Accepted GPT corrections (17 hooks not 18, costs are estimates). Agreed on phased approach and acceptance criteria. Disagreed on Phase 1 scope: proposed including PostToolUse/PreCompact nudges and /update-docs skill (minimal effort, high value). Wrote synthesis proposal and 4 follow-up questions for GPT. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (governance review only)
- 2026-03-01 - ChatGPT (Codex GPT-5) - Reviewed Claude handoff RFC, verified Claude Code documentation links, and added a specific phased proposal for anti-drift enforcement (v1/v1.1/v2), acceptance criteria, and updated priorities/status in HANDOFF. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (governance/rfc refinement only)
- 2026-03-01 - Claude Opus 4.6 - Governance audit + exhaustive Claude Code feature research. Fixed HANDOFF/HISTORY/DECISIONS gaps (stale dates, missing entries, placeholder decisions). Researched Claude Code hook events (initially reported as 18, corrected to 17 in later review), skills, custom agents, rules system, MCP, sandbox. Drafted two-layer anti-drift architecture (portable validation script + Claude Code driver). Wrote detailed cross-LLM handoff for ChatGPT review with documentation URLs and specific review questions. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md, docs/llm/DECISIONS.md, docs/HOOKS_ENFORCEMENT_PROPOSAL.md] - Version impact: no (governance + proposal docs only)
- 2026-02-28 - Claude Opus 4.6 - Add infrastructure plugin open question to HANDOFF. Identified need for generic mechanism to reference central infra docs repo from scaffolded projects. - Files: [docs/llm/HANDOFF.md] - Version impact: no (documentation only)
- 2026-02-22 - Claude Opus 4.6 - Document sync tool system in HOW_TO_USE.md (full usage guide, opt-in steps, CLI reference, troubleshooting). Update docs/STRUCTURE.md with new files. Add HOW_TO_USE.md to README docs table. - Files: [HOW_TO_USE.md, docs/STRUCTURE.md, README.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (documentation only)
- 2026-02-22 - Claude Opus 4.6 - Add dockit-sync tool system for template propagation to downstream projects. New sync manifest, sync script (~1200 lines POSIX sh), check script. Added DOCKIT-TEMPLATE section markers to LLM_START_HERE.md. Bumped to v4.0.0. - Files: [dockit-sync-manifest.yml, scripts/dockit-sync.sh, scripts/dockit-sync-check.sh, LLM_START_HERE.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION] - Version impact: yes (3.0.0 -> 4.0.0, major: requires downstream action to add markers and .dockit-enabled)
